pipeline {
    agent any
      stages {

        stage('Build') {
            steps {
                sh """
                    docker rmi nginx:latest 2>/dev/null || true

                 """

            }
        }

        stage('Deploy') {
            steps {
                sh """
                   sudo systemctl restart ${params.SERVICE}
                """
            }
        }

        }
}
